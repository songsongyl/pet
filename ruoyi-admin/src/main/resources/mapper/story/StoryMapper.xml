<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.story.mapper.StoryMapper">

    <resultMap type="com.ruoyi.story.domain.Story" id="StoryResult">
        <result property="storyId"    column="story_id"    />
        <result property="title"    column="title"    />
        <result property="coverImage"    column="cover_image"    />
        <result property="content"    column="content"    />
        <result property="author"    column="author"    />
        <result property="authorId"    column="author_id"    />
        <result property="likeCount"    column="like_count"    />
        <result property="commentCount"    column="comment_count"    />
        <result property="viewCount"    column="view_count"    />
        <result property="status"    column="status"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="isDeleted"    column="is_deleted"    />
    </resultMap>

    <sql id="selectStoryVo">
        select story_id, title, cover_image, content, author, author_id, like_count, comment_count, view_count, status, create_time, update_time, is_deleted from story
    </sql>

    <select id="selectStoryList" parameterType="com.ruoyi.story.domain.Story" resultMap="StoryResult">
        <include refid="selectStoryVo"/>
        <where>
            <if test="title != null  and title != ''"> and title like concat('%', #{title}, '%')</if>
            <if test="author != null  and author != ''"> and author like concat('%', #{author}, '%')</if>
            <if test="status != null "> and status = #{status}</if>
            <if test="params.beginTime != null and params.beginTime != ''"><!-- 开始时间检索 -->
                and date_format(create_time,'%y%m%d') &gt;= date_format(#{params.beginTime},'%y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''"><!-- 结束时间检索 -->
                and date_format(create_time,'%y%m%d') &lt;= date_format(#{params.endTime},'%y%m%d')
            </if>
            and is_deleted = 0
        </where>
    </select>

    <select id="selectStoryByStoryId" parameterType="Long" resultMap="StoryResult">
        <include refid="selectStoryVo"/>
        where story_id = #{storyId} and is_deleted = 0
    </select>

    <insert id="insertStory" parameterType="com.ruoyi.story.domain.Story" useGeneratedKeys="true" keyProperty="storyId">
        insert into story
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null">title,</if>
            <if test="coverImage != null">cover_image,</if>
            <if test="content != null">content,</if>
            <if test="author != null">author,</if>
            <if test="authorId != null">author_id,</if>
            <if test="likeCount != null">like_count,</if>
            <if test="commentCount != null">comment_count,</if>
            <if test="viewCount != null">view_count,</if>
            <if test="status != null">status,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="isDeleted != null">is_deleted,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null">#{title},</if>
            <if test="coverImage != null">#{coverImage},</if>
            <if test="content != null">#{content},</if>
            <if test="author != null">#{author},</if>
            <if test="authorId != null">#{authorId},</if>
            <if test="likeCount != null">#{likeCount},</if>
            <if test="commentCount != null">#{commentCount},</if>
            <if test="viewCount != null">#{viewCount},</if>
            <if test="status != null">#{status},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="isDeleted != null">#{isDeleted},</if>
        </trim>
    </insert>

    <update id="updateStory" parameterType="com.ruoyi.story.domain.Story">
        update story
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null">title = #{title},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="content != null">content = #{content},</if>
            <if test="author != null">author = #{author},</if>
            <if test="authorId != null">author_id = #{authorId},</if>
            <if test="likeCount != null">like_count = #{likeCount},</if>
            <if test="commentCount != null">comment_count = #{commentCount},</if>
            <if test="viewCount != null">view_count = #{viewCount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="isDeleted != null">is_deleted = #{isDeleted},</if>
        </trim>
        where story_id = #{storyId}
    </update>

    <delete id="deleteStoryByStoryId" parameterType="Long">
        update story set is_deleted = 1 where story_id = #{storyId}
    </delete>

    <delete id="deleteStoryByStoryIds" parameterType="Long">
        update story set is_deleted = 1 where story_id in
        <foreach item="storyId" collection="array" open="(" separator="," close=")">
            #{storyId}
        </foreach>
    </delete>
</mapper>
